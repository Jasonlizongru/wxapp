<view class="section section_gap">
<view class="section__title">输入您想查询的口令：</view>
<view class="section">

</view>
<input class = "studentid" placeholder='输入想查询的口令' bindinput='selectstu'></input>
<view class="section__title">本次课的签到记录是：</view>
<button bindtap='showit'>点击显示名单</button>
<view wx:for="{{objectArray}}" wx:key="key" >
  {{item.stuid}}
  {{item.longitude}}
  {{item.latitude}}
  <!-- {{item.time}} -->
  {{item.notion}}
  {{item.name}}
</view>
</view>

